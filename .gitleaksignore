# Findings gitleaks has already reported and a human has already read.
#
# One line per finding, as `commit:path:rule:line`. Entries are pinned to the commit that
# introduced them, so this file cannot quietly exempt a future leak in the same place: a new
# secret on the same line of the same file is a new commit, a new fingerprint, and a fresh
# failure. Nothing here is a rule-wide or path-wide exemption, deliberately — a `paths`
# allowlist covering `*_test.go` would hide a real key somebody pastes into a fixture, which
# is one of the likelier ways a key actually escapes.
#
# Adding a line here is a claim that the string has no value. Check it before you believe it.
#
# Do not quote the flagged string in a comment here. The first version of this file did, and
# gitleaks flagged the explanation — same rule, same entropy, new fingerprint. Describe it.

# The webhook signing secret in the unit test for Stripe signature verification. It was a
# base64 blob whose plaintext says it is fake; it authenticates nothing and no Stripe account
# has ever held it. generic-api-key matched on entropy alone, not on Stripe's key format. The
# constant was rewritten to something unmistakably inert in the next commit, but the original
# is still in this branch's history, which is what the scan reads — and rewriting published
# history to erase a string that was never a secret is the more dangerous of the two moves.
cec60448a9196d4ca5c60924b1f130eafad8a4c7:services/internal/platform/stripe_hmac_test.go:generic-api-key:12

# The comment that used to explain the entry above, in the commit before it was reworded.
# Quoting the flagged string reproduced it, and a range scan reads the commit that introduced
# a line, not the file as it stands — so deleting the quote fixed the file and not the scan.
# This is the same non-secret, recorded a second time under the fingerprint it was found at.
e498d3e5023d56b9e4a7ae9031dab33e882767a6:.gitleaksignore:generic-api-key:12
