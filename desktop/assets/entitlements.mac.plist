<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	  Electron's V8 needs writable-executable memory for JIT. Without these three the app
	  crashes on launch under the hardened runtime, which is required for notarisation —
	  so they are not optional, they are the cost of shipping a signed Electron app.
	-->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>

	<!-- Outbound network only. The app is a client; it listens on nothing. -->
	<key>com.apple.security.network.client</key>
	<true/>
</dict>
</plist>
