-- Placeholder so a fresh install can walk 57 → 58 → 59 without a hole.
--
-- Pulse v1 is a replica-derived feed of project updates. It adds no tables: the
-- rows already live in project_update. Inbox digests and workspace cadence stay later.

SELECT 1;
