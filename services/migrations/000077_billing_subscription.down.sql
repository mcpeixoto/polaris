-- The workspace plan columns are deliberately not touched. They are older than this table
-- (000016) and a workspace that is on Pro is on Pro whether or not we still hold the
-- provider's record of why.
DROP TABLE IF EXISTS subscription;
