// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: slas.sql

package store

import (
	"context"
	"encoding/json"

	"github.com/google/uuid"
)

const createSlaRule = `-- name: CreateSlaRule :one
INSERT INTO sla_rule (id, workspace_id, position, filter, action, duration_minutes)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, workspace_id, position, filter, action, duration_minutes, created_at, updated_at
`

type CreateSlaRuleParams struct {
	ID              uuid.UUID
	WorkspaceID     uuid.UUID
	Position        string
	Filter          json.RawMessage
	Action          string
	DurationMinutes *int32
}

func (q *Queries) CreateSlaRule(ctx context.Context, arg CreateSlaRuleParams) (SlaRule, error) {
	row := q.db.QueryRow(ctx, createSlaRule,
		arg.ID,
		arg.WorkspaceID,
		arg.Position,
		arg.Filter,
		arg.Action,
		arg.DurationMinutes,
	)
	var i SlaRule
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.Position,
		&i.Filter,
		&i.Action,
		&i.DurationMinutes,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteSlaRule = `-- name: DeleteSlaRule :exec
DELETE FROM sla_rule WHERE id = $1
`

func (q *Queries) DeleteSlaRule(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteSlaRule, id)
	return err
}

const getSlaRule = `-- name: GetSlaRule :one
SELECT id, workspace_id, position, filter, action, duration_minutes, created_at, updated_at
FROM sla_rule
WHERE id = $1
`

func (q *Queries) GetSlaRule(ctx context.Context, id uuid.UUID) (SlaRule, error) {
	row := q.db.QueryRow(ctx, getSlaRule, id)
	var i SlaRule
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.Position,
		&i.Filter,
		&i.Action,
		&i.DurationMinutes,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getSlaRuleForUpdate = `-- name: GetSlaRuleForUpdate :one
SELECT id, workspace_id, position, filter, action, duration_minutes, created_at, updated_at
FROM sla_rule
WHERE id = $1
FOR UPDATE
`

func (q *Queries) GetSlaRuleForUpdate(ctx context.Context, id uuid.UUID) (SlaRule, error) {
	row := q.db.QueryRow(ctx, getSlaRuleForUpdate, id)
	var i SlaRule
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.Position,
		&i.Filter,
		&i.Action,
		&i.DurationMinutes,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const lastSlaRulePosition = `-- name: LastSlaRulePosition :one
SELECT position FROM sla_rule
WHERE workspace_id = $1
ORDER BY position DESC
LIMIT 1
`

func (q *Queries) LastSlaRulePosition(ctx context.Context, workspaceID uuid.UUID) (string, error) {
	row := q.db.QueryRow(ctx, lastSlaRulePosition, workspaceID)
	var position string
	err := row.Scan(&position)
	return position, err
}

const listSlaRulesInWorkspace = `-- name: ListSlaRulesInWorkspace :many
SELECT id, workspace_id, position, filter, action, duration_minutes, created_at, updated_at
FROM sla_rule
WHERE workspace_id = $1
ORDER BY position
`

func (q *Queries) ListSlaRulesInWorkspace(ctx context.Context, workspaceID uuid.UUID) ([]SlaRule, error) {
	rows, err := q.db.Query(ctx, listSlaRulesInWorkspace, workspaceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SlaRule{}
	for rows.Next() {
		var i SlaRule
		if err := rows.Scan(
			&i.ID,
			&i.WorkspaceID,
			&i.Position,
			&i.Filter,
			&i.Action,
			&i.DurationMinutes,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const streamSlaRulesForBootstrap = `-- name: StreamSlaRulesForBootstrap :many
SELECT id, workspace_id, position, filter, action, duration_minutes, created_at, updated_at
FROM sla_rule
WHERE workspace_id = $1
  AND id > $2
ORDER BY id
LIMIT $3
`

type StreamSlaRulesForBootstrapParams struct {
	WorkspaceID uuid.UUID
	AfterID     uuid.UUID
	PageSize    int32
}

func (q *Queries) StreamSlaRulesForBootstrap(ctx context.Context, arg StreamSlaRulesForBootstrapParams) ([]SlaRule, error) {
	rows, err := q.db.Query(ctx, streamSlaRulesForBootstrap, arg.WorkspaceID, arg.AfterID, arg.PageSize)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SlaRule{}
	for rows.Next() {
		var i SlaRule
		if err := rows.Scan(
			&i.ID,
			&i.WorkspaceID,
			&i.Position,
			&i.Filter,
			&i.Action,
			&i.DurationMinutes,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateSlaRule = `-- name: UpdateSlaRule :one
UPDATE sla_rule
SET filter           = COALESCE($1, filter),
    action           = COALESCE($2, action),
    duration_minutes = CASE WHEN $3::boolean THEN $4
                            ELSE duration_minutes END,
    position         = COALESCE($5, position)
WHERE id = $6
RETURNING id, workspace_id, position, filter, action, duration_minutes, created_at, updated_at
`

type UpdateSlaRuleParams struct {
	Filter          []byte
	Action          *string
	SetDuration     bool
	DurationMinutes *int32
	Position        *string
	ID              uuid.UUID
}

func (q *Queries) UpdateSlaRule(ctx context.Context, arg UpdateSlaRuleParams) (SlaRule, error) {
	row := q.db.QueryRow(ctx, updateSlaRule,
		arg.Filter,
		arg.Action,
		arg.SetDuration,
		arg.DurationMinutes,
		arg.Position,
		arg.ID,
	)
	var i SlaRule
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.Position,
		&i.Filter,
		&i.Action,
		&i.DurationMinutes,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
