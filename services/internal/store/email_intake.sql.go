// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: email_intake.sql

package store

import (
	"context"

	"github.com/google/uuid"
)

const getInboundEmailByMessageID = `-- name: GetInboundEmailByMessageID :one
SELECT id, workspace_id, issue_id, message_id, created_at
FROM inbound_email
WHERE workspace_id = $1 AND message_id = $2
`

type GetInboundEmailByMessageIDParams struct {
	WorkspaceID uuid.UUID
	MessageID   string
}

func (q *Queries) GetInboundEmailByMessageID(ctx context.Context, arg GetInboundEmailByMessageIDParams) (InboundEmail, error) {
	row := q.db.QueryRow(ctx, getInboundEmailByMessageID, arg.WorkspaceID, arg.MessageID)
	var i InboundEmail
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.IssueID,
		&i.MessageID,
		&i.CreatedAt,
	)
	return i, err
}

const insertInboundEmail = `-- name: InsertInboundEmail :one

INSERT INTO inbound_email (id, workspace_id, issue_id, message_id)
VALUES ($1, $2, $3, $4)
RETURNING id, workspace_id, issue_id, message_id, created_at
`

type InsertInboundEmailParams struct {
	ID          uuid.UUID
	WorkspaceID uuid.UUID
	IssueID     uuid.UUID
	MessageID   string
}

// Inbound email idempotency: a retried webhook with the same Message-ID must not mint
// a second issue. Replies are refused before they reach this table.
func (q *Queries) InsertInboundEmail(ctx context.Context, arg InsertInboundEmailParams) (InboundEmail, error) {
	row := q.db.QueryRow(ctx, insertInboundEmail,
		arg.ID,
		arg.WorkspaceID,
		arg.IssueID,
		arg.MessageID,
	)
	var i InboundEmail
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.IssueID,
		&i.MessageID,
		&i.CreatedAt,
	)
	return i, err
}
