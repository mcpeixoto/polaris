// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: recurring.sql

package store

import (
	"context"
	"encoding/json"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const advanceRecurringIssue = `-- name: AdvanceRecurringIssue :one
UPDATE recurring_issue
SET next_due_date   = $1,
    last_created_at = $2
WHERE id = $3 AND archived_at IS NULL
RETURNING id, workspace_id, team_id, title, body, properties, template_id, cadence,
          next_due_date, last_created_at, created_by, archived_at, created_at, updated_at
`

type AdvanceRecurringIssueParams struct {
	NextDueDate   pgtype.Date
	LastCreatedAt *time.Time
	ID            uuid.UUID
}

type AdvanceRecurringIssueRow struct {
	ID            uuid.UUID
	WorkspaceID   uuid.UUID
	TeamID        uuid.UUID
	Title         string
	Body          string
	Properties    json.RawMessage
	TemplateID    *uuid.UUID
	Cadence       string
	NextDueDate   pgtype.Date
	LastCreatedAt *time.Time
	CreatedBy     *uuid.UUID
	ArchivedAt    *time.Time
	CreatedAt     time.Time
	UpdatedAt     time.Time
}

// AdvanceRecurringIssue is the mint path: the due date of the occurrence just filed,
// and when it was filed. Separate from UpdateRecurringIssue so a settings edit cannot
// accidentally look like a mint (or the other way around).
func (q *Queries) AdvanceRecurringIssue(ctx context.Context, arg AdvanceRecurringIssueParams) (AdvanceRecurringIssueRow, error) {
	row := q.db.QueryRow(ctx, advanceRecurringIssue, arg.NextDueDate, arg.LastCreatedAt, arg.ID)
	var i AdvanceRecurringIssueRow
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.TeamID,
		&i.Title,
		&i.Body,
		&i.Properties,
		&i.TemplateID,
		&i.Cadence,
		&i.NextDueDate,
		&i.LastCreatedAt,
		&i.CreatedBy,
		&i.ArchivedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const archiveRecurringIssue = `-- name: ArchiveRecurringIssue :one
UPDATE recurring_issue SET archived_at = now()
WHERE id = $1 AND archived_at IS NULL
RETURNING id, workspace_id, team_id, title, body, properties, template_id, cadence,
          next_due_date, last_created_at, created_by, archived_at, created_at, updated_at
`

type ArchiveRecurringIssueRow struct {
	ID            uuid.UUID
	WorkspaceID   uuid.UUID
	TeamID        uuid.UUID
	Title         string
	Body          string
	Properties    json.RawMessage
	TemplateID    *uuid.UUID
	Cadence       string
	NextDueDate   pgtype.Date
	LastCreatedAt *time.Time
	CreatedBy     *uuid.UUID
	ArchivedAt    *time.Time
	CreatedAt     time.Time
	UpdatedAt     time.Time
}

func (q *Queries) ArchiveRecurringIssue(ctx context.Context, id uuid.UUID) (ArchiveRecurringIssueRow, error) {
	row := q.db.QueryRow(ctx, archiveRecurringIssue, id)
	var i ArchiveRecurringIssueRow
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.TeamID,
		&i.Title,
		&i.Body,
		&i.Properties,
		&i.TemplateID,
		&i.Cadence,
		&i.NextDueDate,
		&i.LastCreatedAt,
		&i.CreatedBy,
		&i.ArchivedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const createRecurringIssue = `-- name: CreateRecurringIssue :one

INSERT INTO recurring_issue (id, workspace_id, team_id, title, body, properties,
                             template_id, cadence, next_due_date, last_created_at, created_by)
VALUES ($1, $2, $3, $4,
        COALESCE($5::text, ''), $6,
        $7, $8, $9,
        $10, $11)
RETURNING id, workspace_id, team_id, title, body, properties, template_id, cadence,
          next_due_date, last_created_at, created_by, archived_at, created_at, updated_at
`

type CreateRecurringIssueParams struct {
	ID            uuid.UUID
	WorkspaceID   uuid.UUID
	TeamID        uuid.UUID
	Title         string
	Body          *string
	Properties    json.RawMessage
	TemplateID    *uuid.UUID
	Cadence       string
	NextDueDate   pgtype.Date
	LastCreatedAt *time.Time
	CreatedBy     *uuid.UUID
}

type CreateRecurringIssueRow struct {
	ID            uuid.UUID
	WorkspaceID   uuid.UUID
	TeamID        uuid.UUID
	Title         string
	Body          string
	Properties    json.RawMessage
	TemplateID    *uuid.UUID
	Cadence       string
	NextDueDate   pgtype.Date
	LastCreatedAt *time.Time
	CreatedBy     *uuid.UUID
	ArchivedAt    *time.Time
	CreatedAt     time.Time
	UpdatedAt     time.Time
}

// Recurring issue schedules. Column lists follow the table order, same rule as issues.sql.
func (q *Queries) CreateRecurringIssue(ctx context.Context, arg CreateRecurringIssueParams) (CreateRecurringIssueRow, error) {
	row := q.db.QueryRow(ctx, createRecurringIssue,
		arg.ID,
		arg.WorkspaceID,
		arg.TeamID,
		arg.Title,
		arg.Body,
		arg.Properties,
		arg.TemplateID,
		arg.Cadence,
		arg.NextDueDate,
		arg.LastCreatedAt,
		arg.CreatedBy,
	)
	var i CreateRecurringIssueRow
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.TeamID,
		&i.Title,
		&i.Body,
		&i.Properties,
		&i.TemplateID,
		&i.Cadence,
		&i.NextDueDate,
		&i.LastCreatedAt,
		&i.CreatedBy,
		&i.ArchivedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getRecurringIssue = `-- name: GetRecurringIssue :one
SELECT id, workspace_id, team_id, title, body, properties, template_id, cadence,
       next_due_date, last_created_at, created_by, archived_at, created_at, updated_at
FROM recurring_issue
WHERE id = $1
`

type GetRecurringIssueRow struct {
	ID            uuid.UUID
	WorkspaceID   uuid.UUID
	TeamID        uuid.UUID
	Title         string
	Body          string
	Properties    json.RawMessage
	TemplateID    *uuid.UUID
	Cadence       string
	NextDueDate   pgtype.Date
	LastCreatedAt *time.Time
	CreatedBy     *uuid.UUID
	ArchivedAt    *time.Time
	CreatedAt     time.Time
	UpdatedAt     time.Time
}

func (q *Queries) GetRecurringIssue(ctx context.Context, id uuid.UUID) (GetRecurringIssueRow, error) {
	row := q.db.QueryRow(ctx, getRecurringIssue, id)
	var i GetRecurringIssueRow
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.TeamID,
		&i.Title,
		&i.Body,
		&i.Properties,
		&i.TemplateID,
		&i.Cadence,
		&i.NextDueDate,
		&i.LastCreatedAt,
		&i.CreatedBy,
		&i.ArchivedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getRecurringIssueForUpdate = `-- name: GetRecurringIssueForUpdate :one
SELECT id, workspace_id, team_id, title, body, properties, template_id, cadence,
       next_due_date, last_created_at, created_by, archived_at, created_at, updated_at
FROM recurring_issue
WHERE id = $1
FOR UPDATE
`

type GetRecurringIssueForUpdateRow struct {
	ID            uuid.UUID
	WorkspaceID   uuid.UUID
	TeamID        uuid.UUID
	Title         string
	Body          string
	Properties    json.RawMessage
	TemplateID    *uuid.UUID
	Cadence       string
	NextDueDate   pgtype.Date
	LastCreatedAt *time.Time
	CreatedBy     *uuid.UUID
	ArchivedAt    *time.Time
	CreatedAt     time.Time
	UpdatedAt     time.Time
}

// GetRecurringIssueForUpdate locks the row for a mint pass. Two workers racing on the
// same due date would otherwise both decide it had passed and file two issues.
func (q *Queries) GetRecurringIssueForUpdate(ctx context.Context, id uuid.UUID) (GetRecurringIssueForUpdateRow, error) {
	row := q.db.QueryRow(ctx, getRecurringIssueForUpdate, id)
	var i GetRecurringIssueForUpdateRow
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.TeamID,
		&i.Title,
		&i.Body,
		&i.Properties,
		&i.TemplateID,
		&i.Cadence,
		&i.NextDueDate,
		&i.LastCreatedAt,
		&i.CreatedBy,
		&i.ArchivedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listActiveRecurringIssues = `-- name: ListActiveRecurringIssues :many
SELECT id, workspace_id, team_id, title, body, properties, template_id, cadence,
       next_due_date, last_created_at, created_by, archived_at, created_at, updated_at
FROM recurring_issue
WHERE archived_at IS NULL
ORDER BY team_id, next_due_date, id
`

type ListActiveRecurringIssuesRow struct {
	ID            uuid.UUID
	WorkspaceID   uuid.UUID
	TeamID        uuid.UUID
	Title         string
	Body          string
	Properties    json.RawMessage
	TemplateID    *uuid.UUID
	Cadence       string
	NextDueDate   pgtype.Date
	LastCreatedAt *time.Time
	CreatedBy     *uuid.UUID
	ArchivedAt    *time.Time
	CreatedAt     time.Time
	UpdatedAt     time.Time
}

func (q *Queries) ListActiveRecurringIssues(ctx context.Context) ([]ListActiveRecurringIssuesRow, error) {
	rows, err := q.db.Query(ctx, listActiveRecurringIssues)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListActiveRecurringIssuesRow{}
	for rows.Next() {
		var i ListActiveRecurringIssuesRow
		if err := rows.Scan(
			&i.ID,
			&i.WorkspaceID,
			&i.TeamID,
			&i.Title,
			&i.Body,
			&i.Properties,
			&i.TemplateID,
			&i.Cadence,
			&i.NextDueDate,
			&i.LastCreatedAt,
			&i.CreatedBy,
			&i.ArchivedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRecurringIssuesForTeam = `-- name: ListRecurringIssuesForTeam :many
SELECT id, workspace_id, team_id, title, body, properties, template_id, cadence,
       next_due_date, last_created_at, created_by, archived_at, created_at, updated_at
FROM recurring_issue
WHERE team_id = $1 AND archived_at IS NULL
ORDER BY next_due_date, title
`

type ListRecurringIssuesForTeamRow struct {
	ID            uuid.UUID
	WorkspaceID   uuid.UUID
	TeamID        uuid.UUID
	Title         string
	Body          string
	Properties    json.RawMessage
	TemplateID    *uuid.UUID
	Cadence       string
	NextDueDate   pgtype.Date
	LastCreatedAt *time.Time
	CreatedBy     *uuid.UUID
	ArchivedAt    *time.Time
	CreatedAt     time.Time
	UpdatedAt     time.Time
}

func (q *Queries) ListRecurringIssuesForTeam(ctx context.Context, teamID uuid.UUID) ([]ListRecurringIssuesForTeamRow, error) {
	rows, err := q.db.Query(ctx, listRecurringIssuesForTeam, teamID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRecurringIssuesForTeamRow{}
	for rows.Next() {
		var i ListRecurringIssuesForTeamRow
		if err := rows.Scan(
			&i.ID,
			&i.WorkspaceID,
			&i.TeamID,
			&i.Title,
			&i.Body,
			&i.Properties,
			&i.TemplateID,
			&i.Cadence,
			&i.NextDueDate,
			&i.LastCreatedAt,
			&i.CreatedBy,
			&i.ArchivedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const streamRecurringIssuesForBootstrap = `-- name: StreamRecurringIssuesForBootstrap :many
SELECT id, workspace_id, team_id, title, body, properties, template_id, cadence,
       next_due_date, last_created_at, created_by, archived_at, created_at, updated_at
FROM recurring_issue
WHERE workspace_id = $1
  AND archived_at IS NULL
  AND team_id = ANY($2::uuid[])
  AND id > $3
ORDER BY id
LIMIT $4
`

type StreamRecurringIssuesForBootstrapParams struct {
	WorkspaceID uuid.UUID
	TeamIds     []uuid.UUID
	AfterID     uuid.UUID
	PageSize    int32
}

type StreamRecurringIssuesForBootstrapRow struct {
	ID            uuid.UUID
	WorkspaceID   uuid.UUID
	TeamID        uuid.UUID
	Title         string
	Body          string
	Properties    json.RawMessage
	TemplateID    *uuid.UUID
	Cadence       string
	NextDueDate   pgtype.Date
	LastCreatedAt *time.Time
	CreatedBy     *uuid.UUID
	ArchivedAt    *time.Time
	CreatedAt     time.Time
	UpdatedAt     time.Time
}

// StreamRecurringIssuesForBootstrap feeds the initial snapshot. The predicate is the
// team's: a recurring schedule is team-scoped the same way a cycle is, and the change
// rows are emitted under TeamScope, so the snapshot must not ship a private team's
// schedules to someone who is not in it.
//
// Archived schedules are excluded — archiving emits a delete — even though
// issue.recurring_issue_id may still point at one. That column answers "was this
// minted from a schedule" from the server side; the replica filters on the live
// schedule rows.
func (q *Queries) StreamRecurringIssuesForBootstrap(ctx context.Context, arg StreamRecurringIssuesForBootstrapParams) ([]StreamRecurringIssuesForBootstrapRow, error) {
	rows, err := q.db.Query(ctx, streamRecurringIssuesForBootstrap,
		arg.WorkspaceID,
		arg.TeamIds,
		arg.AfterID,
		arg.PageSize,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []StreamRecurringIssuesForBootstrapRow{}
	for rows.Next() {
		var i StreamRecurringIssuesForBootstrapRow
		if err := rows.Scan(
			&i.ID,
			&i.WorkspaceID,
			&i.TeamID,
			&i.Title,
			&i.Body,
			&i.Properties,
			&i.TemplateID,
			&i.Cadence,
			&i.NextDueDate,
			&i.LastCreatedAt,
			&i.CreatedBy,
			&i.ArchivedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const unarchiveRecurringIssue = `-- name: UnarchiveRecurringIssue :one
UPDATE recurring_issue SET archived_at = NULL
WHERE id = $1 AND archived_at IS NOT NULL
RETURNING id, workspace_id, team_id, title, body, properties, template_id, cadence,
          next_due_date, last_created_at, created_by, archived_at, created_at, updated_at
`

type UnarchiveRecurringIssueRow struct {
	ID            uuid.UUID
	WorkspaceID   uuid.UUID
	TeamID        uuid.UUID
	Title         string
	Body          string
	Properties    json.RawMessage
	TemplateID    *uuid.UUID
	Cadence       string
	NextDueDate   pgtype.Date
	LastCreatedAt *time.Time
	CreatedBy     *uuid.UUID
	ArchivedAt    *time.Time
	CreatedAt     time.Time
	UpdatedAt     time.Time
}

func (q *Queries) UnarchiveRecurringIssue(ctx context.Context, id uuid.UUID) (UnarchiveRecurringIssueRow, error) {
	row := q.db.QueryRow(ctx, unarchiveRecurringIssue, id)
	var i UnarchiveRecurringIssueRow
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.TeamID,
		&i.Title,
		&i.Body,
		&i.Properties,
		&i.TemplateID,
		&i.Cadence,
		&i.NextDueDate,
		&i.LastCreatedAt,
		&i.CreatedBy,
		&i.ArchivedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateRecurringIssue = `-- name: UpdateRecurringIssue :one
UPDATE recurring_issue
SET title         = COALESCE($1, title),
    body          = COALESCE($2, body),
    properties    = COALESCE($3, properties),
    cadence       = COALESCE($4, cadence),
    next_due_date = COALESCE($5, next_due_date)
WHERE id = $6 AND archived_at IS NULL
RETURNING id, workspace_id, team_id, title, body, properties, template_id, cadence,
          next_due_date, last_created_at, created_by, archived_at, created_at, updated_at
`

type UpdateRecurringIssueParams struct {
	Title       *string
	Body        *string
	Properties  []byte
	Cadence     *string
	NextDueDate pgtype.Date
	ID          uuid.UUID
}

type UpdateRecurringIssueRow struct {
	ID            uuid.UUID
	WorkspaceID   uuid.UUID
	TeamID        uuid.UUID
	Title         string
	Body          string
	Properties    json.RawMessage
	TemplateID    *uuid.UUID
	Cadence       string
	NextDueDate   pgtype.Date
	LastCreatedAt *time.Time
	CreatedBy     *uuid.UUID
	ArchivedAt    *time.Time
	CreatedAt     time.Time
	UpdatedAt     time.Time
}

func (q *Queries) UpdateRecurringIssue(ctx context.Context, arg UpdateRecurringIssueParams) (UpdateRecurringIssueRow, error) {
	row := q.db.QueryRow(ctx, updateRecurringIssue,
		arg.Title,
		arg.Body,
		arg.Properties,
		arg.Cadence,
		arg.NextDueDate,
		arg.ID,
	)
	var i UpdateRecurringIssueRow
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.TeamID,
		&i.Title,
		&i.Body,
		&i.Properties,
		&i.TemplateID,
		&i.Cadence,
		&i.NextDueDate,
		&i.LastCreatedAt,
		&i.CreatedBy,
		&i.ArchivedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
